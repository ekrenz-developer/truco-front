import { makeStyles } from "@material-ui/core/styles";

const style = makeStyles({
  roomList: {
    flexWrap: "nowrap",
    padding: 5,
  },
  roomContainer: {
    textAlign: "center",
    marginLeft: 2,
    marginRight: 2,
  },
  roomButton: {
    width: "100%",
    height: "70%",
  },
});

export default style;
