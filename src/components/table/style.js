import { makeStyles } from "@material-ui/core/styles";

const style = makeStyles({
  container: {
    margin: 7,
    height: 100,
    width: 100,
  },
  content: {
    alignItems: "center",
  },
  button: {
    textAlign: "center",
    width: "100%",
    height: "100%",
  },
});

export default style;
